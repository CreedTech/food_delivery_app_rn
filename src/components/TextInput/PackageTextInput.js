import { View, Text } from "react-native";
import React from "react";

const PackageTextInput = () => {
  return (
    <View>
      <Text>PackageTextInput</Text>
    </View>
  );
};

export default PackageTextInput;
